# pytest.ini
[pytest]
markers =
    negative: tests with invalid or negative scenarios
    behavioral: tests that check model behavior (consistency, robustness, etc.)
    fairness: tests that check bias/fairness across groups (e.g., gender, class)